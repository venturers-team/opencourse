# 격차 보고서 (중간본 — 2026-08-25)

goal의 최종 산출물 중 하나. 의도적으로 미룬 것과 그 이유. 정지 지점이 풀릴 때마다 갱신한다.

## 완료 (0~8단계 + 선행분)

- 게이트·격리·보드·전부-아니면-무: 단위 104건 + E2E 15건 + 우회 일습이 CI 상시 증거
- 공개 사이트 전 화면, 접근성 실측 100·100·100, 상태 11종 전수 자동 검사
- S7 렌더러 실물(Playwright), S9 Whisper 준비, 9·10단계 코드 전부(변수만 대기)
- 실전 교재 S1·S2 (S3 승인 대기)

## 정지 지점에 묶인 것 (사용자 결정 대기)

| 항목 | 묶인 이유 | 풀리면 걸릴 시간 |
| --- | --- | --- |
| 7단계 종결 | 디자인 승인 (goal 지정 STOP) | 승인 즉시 |
| 실전 교재 S3~S12 | 목차 승인 + Qwen3-TTS 키(비용) | 반나절 (본문·검수 포함) |
| 9단계 전체 | GitHub·Cloudflare 계정 (docs/13 절차 준비됨) | 1시간 |
| 10단계 전체 | Supabase 생성(서울)·OpenRouter 키(비용)·법률 목록(docs/14) | 반나절 |
| 운영 빌드 우회 재검·후임자 검산·지표 실측 확정 | 위 전부가 선행 조건 | 1~2시간 |

## 의도적 연기 (정지 지점과 무관)

- **TTS 실물 연결 검증**: 어댑터·검증 로직은 테스트됐으나 실제 Qwen3-TTS 호출은 키(비용) 필요 — S9 완주 시점에 함께
- **한도 우회·RLS·주입·초안 격리 통합 시험**: **로컬 Supabase 스택에서 10건 전부 통과** (`pnpm e2e:backend` — 경합 30건 원자성·RLS 교차 거부·11번째 호출 429·주입 필터·초안 문맥 404). 남은 것은 운영 프로젝트 생성 뒤 같은 시험 반복뿐
- **thresholds judgement→measured**: 실측 대상(실전 완주)이 아직 없음 — S12 뒤 실행 기록으로 전환
- **enrich(외부 미디어 큐레이션)**: 1차 범위 제외 (docs/10 결정)
- **관리 GUI**: 범위 제외 확정 (docs/09) — CLI+보드가 대체, 비개발자 관리자 생기면 재고
